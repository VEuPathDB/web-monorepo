#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Check yarn.lock format before allowing commit
./tools/scripts/check-yarn-lockfile.sh || exit 1

# Check .yarnrc.yml configuration before allowing commit
./tools/scripts/check-yarnrc-config.sh || exit 1

yarn lint-staged
