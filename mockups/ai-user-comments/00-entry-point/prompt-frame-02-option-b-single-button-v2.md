# Frame 02 — Option B: single button replacing the text link (v2)

Continue with the same PlasmoDB chrome and style established in the previous frame.

**Base**: same gene page User Comments section as `mockup-frame-01-option-a-three-buttons.png` with the following changes:

**Replace** both outlined buttons from frame 01 with a single outlined button labelled **"+ Add a comment ▾"**. The chevron (▾) is part of the button label text — it sits at the end of the label with a small space before it. There is **no vertical dividing line** inside the button, no split-button pattern, no separator of any kind. It is one continuous button with one uniform background, one border, and the label text "＋ Add a comment ▾" rendered as a single string.

Button style: white background, light gray border (1px solid), dark gray text, small border radius (~4px), compact padding.

The Download and Discuss text links above remain unchanged.

## Annotations

- ① **Single unified button** — the ▾ chevron is part of the label text; no split or dividing line inside the button
- ② **Download / Discuss links unchanged**

Save as: `mockup-frame-02-option-b-single-button.png`
