# Frame 02 — Option B: single button replacing the text link

Continue with the same PlasmoDB chrome and style established in the previous frame.

**Base**: same gene page User Comments section as frame 01, with this different treatment:

**Replace** the "Add a comment ✏️" text link with a single small outlined button labelled **"+ Add a comment"**. Style: compact, light gray border, white background, dark gray text, small border radius — matching the button style introduced in frame 01 but as a single button rather than a row.

No "Beta" badge on the button itself — the bifurcation (regular vs AI-assisted) happens inside a popover that opens when the button is clicked. The button has a small downward-pointing chevron (▾) at its right edge to hint that clicking opens a menu.

The Download and Discuss links above remain unchanged as plain text links.

## Annotations

- ① **Single "+ Add a comment" button** — replaces the text link; chevron signals a dropdown menu will open
- ② **Download / Discuss links unchanged** — these remain as plain text links above

Save as: `mockup-frame-02-option-b-single-button.png`
