# Frame 03 — Option C: existing text link unchanged (popover closed state)

Continue with the same PlasmoDB chrome and style established in the previous frame.

**Base**: same gene page User Comments section as `base-user-comments.png` — this option makes the smallest possible visual change.

The existing **"Add a comment ✏️"** text link is kept exactly as-is in appearance. The only change is that it now behaves as a trigger for a popover menu when clicked rather than navigating directly. To signal this subtly, add a small downward-pointing chevron character (▾) immediately after the pencil icon, in the same teal link color and font size. The link text reads: **"Add a comment ✏️ ▾"**

Everything else — Download, Discuss, the table — is identical to the base screenshot.

## Annotations

- ① **Chevron added to existing link** — only visual change; signals that clicking opens a choice menu rather than navigating directly

Save as: `mockup-frame-03-option-c-text-link-popover-closed.png`
