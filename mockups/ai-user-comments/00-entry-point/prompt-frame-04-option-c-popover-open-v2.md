# Frame 04 — Option C: popover open (v2)

Continue with the same PlasmoDB chrome and style established in the previous frame.

**Base**: use `mockup-frame-03-option-c-text-link-popover-closed.png` with the popover now open. Also refer to `base-active-popover.png` for the popover visual style — plain white rectangle, subtle drop shadow, no arrow, no border.

**Popover position**: the popover appears directly over and on top of the "Add a comment ✏️ ▾" trigger link, obscuring it completely. The trigger link is not visible — the popover covers it. The popover's top-left corner aligns with where the trigger link was.

**Popover content**: two options stacked vertically, with comfortable padding (≈12px horizontal, 8px vertical per item):

1. **"Add a comment"** — plain dark gray text
2. **"Add an AI-assisted comment from a publication"** — plain dark gray text, with a small teal rounded pill badge reading **"Beta"** immediately after the label

A thin light-gray separator line sits between the two items. The popover is wide enough to fit the second option without wrapping (approximately 310px).

## Annotations

Use plain numbered circles only — no text bubbles. Three annotations, numbered sequentially 1, 2, 3:

- ① points to the popover panel itself
- ② points to the "Add a comment" option
- ③ points to the "Add an AI-assisted comment from a publication" option and its Beta badge

Save as: `mockup-frame-04-option-c-popover-open.png`
