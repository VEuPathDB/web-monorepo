worker_processes 5;
error_log /var/log/nginx_errors.log;
pid nginx.pid;

events {
    worker_connections 4096;
}

http {
    server {
        listen 80;
        location / {
            root /var/www;
        }
    }
}