This project is a component library for plots used in VEuPathDB websites. It uses storybook as a dev environment. To start, clone this repo and run the following commands:

    yarn install
    yarn storybook